# Bun configuration for atreides-opencode

[install]
# Use exact versions for reproducible builds
exact = true

[test]
preload = ["./test/setup.ts"]
coverage = true
coverageDir = "coverage"
coverageThreshold = { line = 80, function = 80, statement = 80 }

[run]
# Bun runtime settings
silent = false
